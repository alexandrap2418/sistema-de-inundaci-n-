<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Política de Seguridad de Contenido para desarrollo -->
    <meta http-equiv="Content-Security-Policy" content="
        default-src 'self'; 
        connect-src 'self' http://127.0.0.1:5500 ws://127.0.0.1:5500;
        style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/ https://fonts.googleapis.com; 
        font-src 'self' https://fonts.gstatic.com; 
        script-src 'self' 'unsafe-inline' 'unsafe-eval';
        img-src 'self' data:;">
    <title>Contacto - Sistema de Detección de Inundaciones</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        /* Reset y estilos base */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
            line-height: 1.6;
        }

        /* Header mejorado */
        .header-container {
            background: linear-gradient(135deg, #1e3c72, #2a5298);
            text-align: center;
            padding: 2rem 1rem;
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .header-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 20% 50%, rgba(255,255,255,0.05) 0%, transparent 50%),
                        radial-gradient(circle at 80% 20%, rgba(255,255,255,0.05) 0%, transparent 50%),
                        radial-gradient(circle at 40% 80%, rgba(255,255,255,0.05) 0%, transparent 50%);
            animation: float 20s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }

        .header-title {
            color: white;
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.3);
            position: relative;
            z-index: 2;
        }

        .water-drop {
            display: inline-block;
            animation: bounce 2s ease-in-out infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-15px); }
        }

        /* Navegación */
        .main-nav {
            display: flex;
            justify-content: center;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 1rem;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .main-nav a {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem 1.5rem;
            text-decoration: none;
            color: #333;
            border-radius: 50px;
            margin: 0.25rem;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .main-nav a:hover {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(102, 126, 234, 0.3);
        }

        .main-nav a.active {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        .main-nav i {
            font-size: 1.1rem;
        }

        /* Contenedor principal */
        .contact-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem;
        }

        .contact-header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .contact-header h2 {
            color: white;
            font-size: 2.5rem;
            margin-bottom: 1rem;
            text-shadow: 2px 2px 10px rgba(0,0,0,0.3);
        }

        .contact-header .subtitle {
            color: rgba(255, 255, 255, 0.9);
            font-size: 1.2rem;
            font-weight: 300;
        }

        /* Formulario */
        .form-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            margin-bottom: 3rem;
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
        }

        .form-group {
            position: relative;
            transition: transform 0.3s ease;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-group label {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
            font-size: 1rem;
        }

        .form-group i {
            color: #667eea;
            font-size: 1.1rem;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 1rem;
            font-family: inherit;
            transition: all 0.3s ease;
            background: white;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
            transform: translateY(-2px);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .submit-btn {
            width: 100%;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            padding: 1.2rem 2rem;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(102, 126, 234, 0.4);
            background: linear-gradient(135deg, #5a67d8, #6b46c1);
        }

        /* Información de contacto */
        .contact-info {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .info-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            padding: 2rem;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .info-card:hover {
            transform: translateY(-5px);
        }

        .info-icon {
            background: linear-gradient(135deg, #667eea, #764ba2);
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
        }

        .info-icon i {
            color: white;
            font-size: 1.5rem;
        }

        .info-content h4 {
            color: #333;
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
            font-weight: 600;
        }

        .info-content p {
            color: #666;
            font-size: 1rem;
        }

        /* Mensaje de éxito */
        .success-message {
            position: fixed;
            top: 20px;
            right: 20px;
            background: linear-gradient(135deg, #10B981, #059669);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(16, 185, 129, 0.3);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            transform: translateX(400px);
            opacity: 0;
            transition: all 0.5s ease;
            z-index: 1000;
        }

        .success-message.show {
            transform: translateX(0);
            opacity: 1;
        }

        .success-message i {
            font-size: 1.2rem;
        }

        /* Footer */
        .footer-credits {
            text-align: center;
            padding: 2rem;
            color: white;
            background: rgba(0,0,0,0.2);
            margin-top: 3rem;
            border-radius: 15px 15px 0 0;
        }

        .footer-credits p {
            font-size: 1rem;
            font-weight: 300;
        }

        /* Redes sociales */
        .social-sidebar {
            position: fixed;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(15px);
            border-radius: 15px;
            padding: 1rem;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            z-index: 1000;
        }

        .social-title {
            text-align: center;
            font-weight: 600;
            color: #333;
            margin-bottom: 1rem;
            font-size: 0.9rem;
        }

        .social-sidebar ul {
            list-style: none;
        }

        .social-sidebar li {
            margin-bottom: 0.5rem;
        }

        .social-link {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.75rem;
            text-decoration: none;
            color: #333;
            border-radius: 10px;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }

        .social-link:hover {
            transform: translateX(5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .social-link.facebook:hover { background: #1877F2; color: white; }
        .social-link.twitter:hover { background: #1DA1F2; color: white; }
        .social-link.instagram:hover { background: #E4405F; color: white; }
        .social-link.email:hover { background: #EA4335; color: white; }
        .social-link.whatsapp:hover { background: #25D366; color: white; }

        /* Responsive Design */
        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }
            
            .contact-container {
                padding: 1rem;
            }
            
            .form-container {
                padding: 2rem;
            }
            
            .contact-header h2 {
                font-size: 2rem;
            }
            
            .header-title {
                font-size: 2rem;
            }
            
            .social-sidebar {
                position: static;
                transform: none;
                margin: 2rem auto;
                max-width: 300px;
            }
            
            .success-message {
                left: 20px;
                right: 20px;
                transform: translateY(100px);
            }
            
            .success-message.show {
                transform: translateY(0);
            }
        }

        @media (max-width: 480px) {
            .header-container {
                padding: 1.5rem 1rem;
            }
            
            .header-title {
                font-size: 1.5rem;
            }
            
            .contact-header h2 {
                font-size: 1.5rem;
            }
            
            .form-container {
                padding: 1.5rem;
            }
            
            .main-nav a {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <div class="header-container">
        <h1 class="header-title">FLOOD DETECTION SYSTEM</h1>
        <div class="water-drop">
            <svg width="70" height="70" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
                <defs>
                    <linearGradient id="waterGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                        <stop offset="0%" style="stop-color:#00BFFF;stop-opacity:1" />
                        <stop offset="100%" style="stop-color:#0080FF;stop-opacity:1" />
                    </linearGradient>
                </defs>
                <path d="M50 10 C65 35, 85 55, 50 90 C15 55, 35 35, 50 10 Z" fill="url(#waterGrad)" stroke="#ffffff" stroke-width="2"/>
                <path d="M20 75 Q35 65, 50 75 T80 75" fill="none" stroke="#ffffff" stroke-width="3"/>
            </svg>
        </div>
    </div>

    <!-- Navegación -->
    <nav class="main-nav">
        <a href="index.html" data-page="index">
            <i class="fas fa-home"></i>
            <span>Inicio</span>
        </a>
        <a href="Mision.html" data-page="mision">
            <i class="fas fa-bullseye"></i>
            <span>Misión y Visión</span>
        </a>
        <a href="resenas.html" data-page="resenas">
            <i class="fas fa-star"></i>
            <span>Reseñas</span>
        </a>
        <a href="Contacto.html" data-page="contacto" class="active">
            <i class="fas fa-envelope"></i>
            <span>Contacto</span>
        </a>
    </nav>

    <!-- Contenido principal -->
    <div class="contact-container">
        <div class="contact-header">
            <h2><i class="fas fa-comments"></i> Cuéntanos tu Opinión</h2>
            <p class="subtitle">Nos encanta escuchar a nuestra comunidad</p>
        </div>

        <div class="form-container">
            <form id="contact-form">
                <div class="form-grid">
                    <div class="form-group">
                        <label for="nombre">
                            <i class="fas fa-user"></i>
                            Nombre Completo
                        </label>
                        <input type="text" id="nombre" name="nombre" placeholder="¿Cómo te llamas?" required>
                    </div>
                    
                    <div class="form-group">
                        <label for="correo">
                            <i class="fas fa-envelope"></i>
                            Correo Electrónico
                        </label>
                        <input type="email" id="correo" name="correo" placeholder="tu@email.com" required>
                    </div>
                    
                    <div class="form-group full-width">
                        <label for="mensaje">
                            <i class="fas fa-comment-dots"></i>
                            Tu Mensaje
                        </label>
                        <textarea id="mensaje" name="mensaje" placeholder="¡Cuéntanos algo! Tu opinión es muy importante para nosotros..." required></textarea>
                    </div>
                    
                    <div class="form-group full-width">
                        <button type="submit" class="submit-btn">
                            <i class="fas fa-paper-plane"></i>
                            Enviar Mensaje
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <!-- Información adicional -->
        <div class="contact-info">
            <div class="info-card">
                <div class="info-icon">
                    <i class="fas fa-map-marker-alt"></i>
                </div>
                <div class="info-content">
                    <h4>Ubicación</h4>
                    <p>Medellín, Antioquia, Colombia</p>
                </div>
            </div>
            
            <div class="info-card">
                <div class="info-icon">
                    <i class="fas fa-envelope"></i>
                </div>
                <div class="info-content">
                    <h4>Email</h4>
                    <p>Alm4n4k3@gmail.com</p>
                </div>
            </div>
            
            <div class="info-card">
                <div class="info-icon">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="info-content">
                    <h4>Respuesta</h4>
                    <p>En 24 horas o menos</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Mensaje de éxito -->
    <div id="success-message" class="success-message">
        <i class="fas fa-check-circle"></i>
        <p>¡Mensaje enviado! Te responderemos pronto.</p>
    </div>

    <!-- Footer -->
    <div class="footer-credits">
        <p><i class="fas fa-code"></i> Desarrollado con 💙 por <strong>Alexandra Pulgarín</strong> y <strong>Sara Usuga</strong></p>
    </div>

    <!-- Redes sociales -->
    <div class="social-sidebar">
        <div class="social-title">Síguenos</div>
        <ul>
            <li>
                <a href="https://www.facebook.com" target="_blank" class="social-link facebook">
                    <i class="fab fa-facebook-f"></i>
                    <span>Facebook</span>
                </a>
            </li>
            <li>
                <a href="https://www.twitter.com" target="_blank" class="social-link twitter">
                    <i class="fab fa-twitter"></i>
                    <span>Twitter</span>
                </a>
            </li>
            <li>
                <a href="https://www.instagram.com" target="_blank" class="social-link instagram">
                    <i class="fab fa-instagram"></i>
                    <span>Instagram</span>
                </a>
            </li>
            <li>
                <a href="mailto:Alm4n4k3@gmail.com" target="_blank" class="social-link email">
                    <i class="fas fa-envelope"></i>
                    <span>Email</span>
                </a>
            </li>
            <li>
                <a href="https://www.whatsapp.com" target="_blank" class="social-link whatsapp">
                    <i class="fab fa-whatsapp"></i>
                    <span>WhatsApp</span>
                </a>
            </li>
        </ul>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const contactForm = document.getElementById('contact-form');
            const successMessage = document.getElementById('success-message');
            
            // Establecer navegación activa
            function setActiveNavigation() {
                const navLinks = document.querySelectorAll('.main-nav a');
                navLinks.forEach(link => link.classList.remove('active'));
                
                const contactLink = document.querySelector('[data-page="contacto"]');
                if (contactLink) contactLink.classList.add('active');
            }
            
            setActiveNavigation();
            
            // Manejar envío del formulario
            contactForm.addEventListener('submit', function(e) {
                e.preventDefault();
                
                const formData = new FormData(contactForm);
                const nombre = formData.get('nombre');
                const correo = formData.get('correo');
                const mensaje = formData.get('mensaje');
                
                // Validación
                if (!nombre.trim() || !correo.trim() || !mensaje.trim()) {
                    alert('Por favor, completa todos los campos');
                    return;
                }
                
                if (!isValidEmail(correo)) {
                    alert('Por favor, ingresa un correo válido');
                    return;
                }
                
                console.log('Datos del formulario:', { nombre, correo, mensaje });
                showSuccessMessage();
                contactForm.reset();
            });
            
            // Función para mostrar mensaje de éxito
            function showSuccessMessage() {
                successMessage.classList.add('show');
                setTimeout(() => {
                    successMessage.classList.remove('show');
                }, 4000);
            }
            
            // Validar email
            function isValidEmail(email) {
                const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                return emailRegex.test(email);
            }
            
            // Efectos de foco en inputs
            const inputs = document.querySelectorAll('input, textarea');
            inputs.forEach(input => {
                input.addEventListener('focus', function() {
                    this.parentElement.style.transform = 'scale(1.02)';
                });
                
                input.addEventListener('blur', function() {
                    this.parentElement.style.transform = 'scale(1)';
                });
            });
            
            // Animación de carga
            setTimeout(() => {
                document.body.style.opacity = '1';
            }, 100);
        });
    </script>
</body>
</html>